# E2E walkthrough bug log

## Fixed (blocking, committed to develop)

| # | Stage | Fix | Note |
|---|---|---|---|
| 5 | Phase 2, /ck:debug session | Added `AbortSignal.timeout(10_000)` to `graph-client.ts` (`getToken` + `sendMail` fetch) and `brevo-client.ts` (`sendViaBrevo` fetch) | Genuine hardening gap found while investigating #4: neither MSAL's `getToken()` nor the raw `fetch()` calls to Graph/Brevo had any timeout — an unresponsive Entra/Graph/Brevo endpoint could hang the outbox worker (and its `workerRunning` module-level lock) forever, in prod too, not just locally. Isolated reproduction (token-only, token+send, full `runEmailOutbox()`) all completed in 1-3s with NO hang — could not confirm this was THE root cause of #4, but it's a real defensive gap worth fixing regardless (user-approved as "vệ sinh code" 2026-07-05). Verified: `tsc --noEmit` clean, `email-graph-client.test.ts` (8/8 pass), `brevo-client.test.ts` (4/5 pass — the 1 failure is pre-existing, reproduced identically on `git stash` of this fix, unrelated test-isolation bug, see backlog #6). |
| 9 | Phase 3 stage 5 (receipt approve) | **Real data bug**: `finance.ts` `receiptApprove` — both the ParentAccount-email-propagate check and the `lms_account_ready` notify-email check read only the stale `receipt.parentEmail` DB column, ignoring `input.parentEmail` (the value the ke_toan/director types into the "Duyệt phiếu thu — email phụ huynh" dialog when the receipt was created without an email). Result: approving with an approve-time email correctly sets `ParentAccount.email` (a separate code path, line ~732, already resolved `input.parentEmail ?? receipt.parentEmail` correctly) but **silently never enqueues the parent's LMS welcome email** — the exact scenario that dialog exists for. Found live: DB showed `parent_account.email = manhquy.mqy@gmail.com` set correctly but zero `email_outbox` rows for that address after approving. Fixed both checks to resolve `input.parentEmail ?? receipt.parentEmail`, matching the existing pattern. Verified: `tsc --noEmit` clean; ran 23 related integration tests (`lms-student-account-provisioning`, `onboarding-to-lms-timeline`, `email-otp-login`, `email-outbox-router`) — all 23 pass, no regression (none of them covered the approve-time-email path specifically, which is why this shipped unnoticed). Committed `5a225a6`. |
| 7 | Phase 2 stage 1 | Overview widget "Không tải được tổng quan" for giao_vien/cskh | Investigated 2026-07-05: `nav-permissions.ts`'s `overview` gate (added in an earlier commit, `ed9a082`) already correctly hides the sidebar link for these roles — the sidebar-click path was already fixed. But `App.tsx`'s `activeSection`/`knownSection` derivation only checked URL-membership (`ALL_SECTION_KEYS.has(...)`), never re-checking the section's own permission gate — so a direct URL/bookmark/back-forward to `/overview` still bypassed the hidden link and hit the same FORBIDDEN error. Fixed: added `isReachableSection()` in `App.tsx` (mirrors `shell.tsx`'s `visible()`, checks `NAV_GATES[key]` + `can()`) so an unreachable direct URL now falls through to `defaultSection(me)` same as an unknown section. Verified via code-reviewer: giao_vien/cskh correctly redirected, directors/super_admin correctly reach `/overview`, no circular import, `tsc` clean both packages, all 4 nav test files (28 tests) still pass. |
| 8 | Phase 2 stage 3 | "Buổi học" stale after "Sinh lịch" | Fixed 2026-07-05: `class-workspace.tsx`'s `ScheduleTab` and `SessionsTab` are sibling components with independent local state — `generateSessions` succeeding never told `SessionsTab` to refetch. Added `onSessionsGenerated` callback prop from `ScheduleTab` up to the parent `ClassDetail`, which bumps a `sessionsReloadKey` state and passes it as `key={sessionsReloadKey}` on `<SessionsTab>` — remounting it re-fires its `useEffect(load, [load])`. Mirrors the existing `key={reloadKey}` pattern already used for `ReceiptsCard` in `finance-panel.tsx`. Verified: `tsc` clean, prop-threading confirmed correct by code-reviewer, no disruptive state loss (only effect: an open `CreateMakeupSessionModal` would close if a colleague generates sessions at that exact moment — accepted as rare/low-impact). No automated test added (apps/admin has zero component/DOM tests anywhere — consistent with existing project practice, not a new regression risk). |
| 10 | Phase 3 stage 5 (receipt approve) | `receiptApprove` raw 500 on parentEmail collision between different families | Fixed 2026-07-05, in two passes. **First pass** wrapped the raw `parent_account` INSERT in try/catch, retrying without email on a unique-violation — looked correct but code-review caught a critical defect: Postgres aborts the *entire* transaction after any statement error (`25P02`), so the bare-catch retry was itself the next statement inside the aborted transaction and would fail again, rolling back the whole receipt approval (worse than the original bug — same class of defect existed, unnoticed, in the pre-existing sibling `propagatedEmail` catch too, added in `5a225a6`). **Second pass (the actual fix)**: added `SAVEPOINT`/`ROLLBACK TO SAVEPOINT` around both catches so the retry runs in a clean transaction state; added `emailCollisionHandled` flag so the propagatedEmail block skips a redundant duplicate audit-log entry when the insert-catch already handled the same collision. Verified empirically (not just reasoned about) via a new regression test, `student-provisioning-edge-cases.int.test.ts` "EC-EMAIL" — two different-phone receipts sharing one email, second approve must not 500 and must still commit the student+guardian (proof the transaction actually recovered, not just that the exception didn't leak). Code-reviewer confirmed savepoint pair is correct, no orphan-savepoint risk on the non-collision path, full targeted suite green (68 tests, 6 files). |

## Backlog (minor/UX, not fixed this session)

| # | Stage | Issue | Note |
|---|---|---|---|
| 1 | Phase 1 setup | `.env` missing `STAFF_PASSWORD_LOGIN`/`SSO_ENABLED`/`BREVO_API_KEY`/`BREVO_SENDER_EMAIL` keys entirely (not just falsy) | Not a product bug — local `.env` was never fully populated from `.env.example`. Added the 4 keys locally for this session. |
| 2 | Phase 1 setup | `pnpm --filter @cmc/db seed:bootstrap` fails on Windows (`SEED_MODE=bootstrap tsx ...` — POSIX env-var syntax not supported by pnpm's cmd.exe script runner) | Real cross-platform gap — repo's package.json scripts use POSIX `VAR=x cmd` syntax without `cross-env`. Worked around by exporting env vars in bash then calling `npx tsx` directly. Consider adding `cross-env` if Windows-native dev (non-WSL/Git-Bash) is a supported workflow. |
| 3 | Phase 1 setup | `pnpm --filter <pkg> exec prisma ...` does not pick up root `.env` (`DIRECT_URL` not found) | Prisma CLI only auto-loads `.env` from cwd/schema dir, not workspace root, when invoked via `pnpm --filter ... exec`. Worked around by `source .env` in the same shell before invoking. Matches pattern already used in `scripts/ci-integration-tests.sh`. Not a bug, just undocumented for local ad-hoc CLI use outside the documented `pnpm db:migrate`/`pnpm db:seed` root scripts. |

## HIGH-SEVERITY — needs dedicated debug session

| # | Stage | Issue | Evidence | Note |
|---|---|---|---|---|
| 4 | Phase 2, recurring | **API (`localhost:4000`) hangs completely** (all requests, including plain `/` and `auth.me`, time out — no HTTP response at all) after ~15-20 min of normal dev-stack uptime. Recurred twice independently in this session. | (a) First occurrence: right after clicking "Đặt lại mật khẩu" (setPassword mutation) for the 3rd staff account. Restarted stack, retried the *exact same action* — succeeded cleanly, no repro. (b) Second occurrence: ~10 min later, right after logout + page reload (`auth.me` call) — unrelated to setPassword, ruling out that mutation as the direct cause. (c) Ruled out: CPU (process ~0% additional CPU accrued while hung, 16 cores, ~40% overall system load — not starved), DB (`pg_stat_activity` showed 1 active + 9 idle connections, no locks/long queries, well under `max_connections=100`), Prisma pool config (no `connection_limit` override found; `withRls` in `packages/db/src/index.ts:44-74` uses a single top-level `prisma.$transaction` per call, no nested transactions observed in the code read so far). (d) `netstat` showed multiple server-side sockets stuck in `CLOSE_WAIT` on port 4000 — consistent with a request handler that started but never completed / never wrote a response, for requests unrelated to the one that "caused" the hang (i.e. it looks like the whole event loop or a shared resource got stuck, not just one slow request). (e) API's pino logger does not log a line per request (only a startup line was ever seen), so we could not see which specific handler was mid-flight when it froze. | **`/ck:debug` follow-up (2026-07-05):** leading hypothesis was the embedded cron (`index.ts:511-517`, `runEmailOutbox` every minute, same process as the API) hanging on Microsoft Graph calls with real Entra credentials found in local `.env` (Phase 1 preflight only checked `BREVO_API_KEY`, missed `GRAPH_*`/`ENTRA_*` — a real gap in my own preflight). Supporting evidence: 3 `email_outbox` rows stuck in `sending` status matching the hang window. **Hypothesis refuted by 3 isolated reproduction attempts** (token acquisition alone, token+send, full `runEmailOutbox()` against the real app's prisma client) — all completed cleanly in 1-3s, no hang. The 3 stuck rows are more likely an artifact of this session's own `taskkill /F` mid-transaction, not evidence of an app-level hang. Added defensive timeouts to Graph/Brevo clients anyway (see Fixed #5) since the zero-timeout gap is real regardless. **True root cause remains unconfirmed** — most likely either (i) a rare concurrency/timing issue only reproducible under real concurrent browser traffic (not a single-shot script), or (ii) an environment-level hiccup (Windows Defender scanning `node.exe`, Docker Desktop/WSL2 stall) unrelated to app code. Workaround: kill all `node.exe` processes under `D:\project\CMCnew` (turbo/vite/tsx-watch) and `pnpm dev` again — takes ~10-15s to recover, data in Postgres is untouched both times. User decision (2026-07-05): apply the defensive timeout fix and continue the E2E walkthrough with restart-on-hang as the workaround, rather than continuing to chase reproduction. |
| 6 | /ck:debug session | `apps/api/test/brevo-client.test.ts` "POSTs with the api-key header..." fails pre-existing, unrelated to this session's changes (reproduced identically via `git stash`) — expects `'k-123'`, receives `'not-a-real-key-123'` | Test-isolation bug (likely env/mock leakage between test files in the same run), not caused by the Graph/Brevo timeout fix. Not investigated further — out of scope for this E2E session. |

## Backlog (minor, edge case, not fixed this session) — found while live-verifying fix #9

| # | Stage | Issue | Note |
|---|---|---|---|
| 10 | Phase 3 stage 5 | `receiptApprove` crashes with a raw HTTP 500 (`Invalid prisma.$executeRaw() invocation ... Unique constraint failed`) if the ke_toan supplies, at approve time, a `parentEmail` that's already claimed by a DIFFERENT parent (different phone) | `ParentAccount.email` is `@unique` (schema.prisma:554). The new-ParentAccount raw INSERT (finance.ts ~line 734) only has `ON CONFLICT ("phone") DO NOTHING` — it has no graceful handling for an email collision on a phone that ISN'T a conflict. Reproduced 3× live (different receipt ids, same reused test email `manhquy.mqy@gmail.com` for 2 distinct new-parent phones). Real-world impact is low (two genuinely-different families sharing an email is rare, likely a data-entry mistake), but the failure mode is an ugly raw-SQL error surfaced to the ke_toan instead of a friendly "email đã được dùng bởi phụ huynh khác" message — and the transaction rolls back cleanly (no partial/corrupt state), so it's non-blocking. Worth a `try/catch` around that insert (same pattern already used for the *existing*-parentAcc email-propagate path a few lines below) in a future pass. |

## Tooling artifact (NOT a product bug) — found while testing evidence publish

- `chrome-devtools` MCP's `fill`/`fill_form` tools set the DOM `value` of the "Tóm tắt gửi LMS" `Textarea` (Mantine) without the React controlled-component state (`summary` in `session-evidence-panel.tsx`) picking it up — snapshot/screenshot showed the typed text, but `Publish LMS`/`Lưu nháp` sent `summary: ''` to `sessionEvidence.upsertDraft`, failing validation ("Cần có tóm tắt buổi học trước khi publish") or silently saving an empty summary. Confirmed via DB (`session_evidence.summary` empty after 2 `fill_form` attempts). Switching to real keystroke `type_text` fixed it immediately — DB then showed the correct summary, `status='published'`, photo + per-student comments all persisted (`session_evidence`, `session_evidence_photo`, `session_student_comment` all correct). No code was touched; documenting here only so a future session doesn't misdiagnose this as an app bug when using browser automation on this specific field.

## Notes

- Brevo API key confirmed empty in local `.env` → Phase 3 stage 6 (parent email) falls back to outbox-row verification instead of a real send, per Phase 1 preflight decision.
- Seeded directors (`giam_doc_kinh_doanh`, `giam_doc_dao_tao`) exist from `seed:bootstrap` but are marked SSO-only — resolves Plan's open question #1 (directors ARE seeded). Password-login functional proof deferred to Phase 2 stage 1 with newly-created staff.
