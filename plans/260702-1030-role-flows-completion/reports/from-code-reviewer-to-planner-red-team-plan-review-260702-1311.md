# Red-team Plan Review — 260702-1030 role-flows-completion

Reviewer: code-reviewer (adversarial pass, execution-focused; D1–D6 decisions NOT re-litigated).
Baseline: CURRENT working tree on `develop` (includes uncommitted Plan-1 seam-fixes implementation). Every finding verified against file:line.

## Verdict: **FIX-FIRST**

The decisions are sound and most anchors are accurate, but P4's money math has no landing zone (schema + domain package missing from ownership), the override path silently wipes the penalty it introduces, P3's auto-O5 breaks the WON invariant and has no cancel-path unwind, and two "rely on existing guard" claims (director blanket approve, history drill-down) are contradicted by the code. Fix the plan text before implementation.

## Findings

| # | Sev | Phase | Finding | Evidence |
|---|-----|-------|---------|----------|
| C1 | CRITICAL | P4/P1 | **Penalty deduction line has nowhere to live.** `Payslip` has no penalty/other-deduction column (`schema.prisma:1447-1477`) and `assemblePayslip` has no deduction input — `netIncome = gross − insurance − PIT` only (`packages/domain-payroll/src/payslip.ts:69-94`). plan.md gives schema+migration ownership to **P1 only**, and P1's scope is 4 EmploymentProfile columns; P4's file list has neither `schema.prisma` nor `packages/domain-payroll`. As written P4 is unimplementable without violating plan ownership. Also undecided: is the penalty pre-tax (reduces taxable) or post-tax (after PIT)? That is a PIT-affecting money decision that must be pinned in decision 0025. | schema.prisma:1447; payslip.ts:77,93; plan.md §File ownership |
| C2 | CRITICAL | P4 | **Override path wipes (or double-counts) the penalty.** `payslipOverrideVariablePay` recomputes the ENTIRE slip via `assembleSlipData` (`payroll.ts:793-828`, upsert of all fields at :814ff). If penalty aggregation is added "into payslipCompute" as the phase text says, any variable-pay override issued after compute silently rebuilds the slip without the penalty line; conversely, implementing the director "miễn/giảm" override as a manual variablePay tweak conflates commission with penalty. Requirement: penalty aggregation must live inside `assembleSlipData` (single source of truth, same as commission auto-feed at :183-206), with its own override field + reason, not the variablePay mechanism. | payroll.ts:110-244, 751-828 |
| M1 | MAJOR | P4/P5(D5) | **"Directors keep blanket approve via role" is FALSE.** `assertAssignedApprover` (`shift-registration.ts:86-101`) checks only `managerId`/`nextManagerId`/super_admin — there is no director-role bypass. Today directors pass only because `resolveManager` auto-assigns the group's director. Once P2 sets `managerId` = a team lead, the lead's manager or one group director is `nextManagerId` (:41-44); the **other** director gets FORBIDDEN. Decision 0027 text ("both directors approve all") contradicts the guard the plan says to "rely on". Either add a director bypass to the guard (and to `shiftRegistrationPendingItems`, `dashboard.ts:84-96`, which filters the same way) or rewrite 0027 to "assigned manager + next-level only". Test matrix must include "non-assigned director denied/allowed" explicitly. | shift-registration.ts:86-101,41-44; dashboard.ts:90-92 |
| M2 | MAJOR | P3 | **Auto-O5 without `closedAt` breaks the WON invariant.** "Won" in CRM = `stage O5_ENROLLED && closedAt set` — that pair drives the anti-regression guard (`crm.ts:358`) and anti-markLost guard (`crm.ts:390`); `opportunityTransition` stamps `closedAt` + clears `lostReason` on advance (`crm.ts:365-366`). P3 step 4 says only `tx.opportunity.update` stage→O5 + logEvent. A stage-only update yields O5 without closedAt: regressable, markLost-able, invisible to closedAt-based won metrics. Additionally the opp select at `finance.ts:612-617` pulls only `ownerId, stage, studentName` — `closedAt`/`lostReason` needed to evaluate the plan's own "not cancelled/lost" precondition are not fetched. Note "lost" is closedAt+lostReason with stage frozen at e.g. O4 (`crm.ts:398`) — a lost opp with matching studentName currently survives the attribution guard and would be auto-"won". Extend select; replicate won semantics; skip advance when `closedAt && lostReason`. | crm.ts:355-425; finance.ts:612-617 |
| M3 | MAJOR | P3 | **receiptCancel leaves an unrecoverable phantom-won opp.** Cancel refunds voucher + rolls back student/enrollment (`finance.ts:737-843`) but never touches the opportunity. After auto-O5, cancelling the receipt strands the opp at O5+closedAt with zero revenue, and the anti-regression guard (`crm.ts:358`) blocks manual correction. Commission claw-back is handled by status filtering (comment `finance.ts:735-736`) but stage is not. Plan needs a cancel-path rule (e.g. revert auto-advanced opp when the cancelled receipt was the only approved receipt on it, or at minimum allow manual regression of a receipt-less O5). | finance.ts:727-845; crm.ts:355-360 |
| M4 | MAJOR | P2 | **Dependency on seam-fixes P5 forms is currently false.** Plan-1 P5 is marked "implemented", but grep of the whole working tree finds ZERO admin-UI callers of `payroll.profileUpsert`/`rateCreate` (only API + tests; `staff-profile.tsx` calls profileList/rateList/listByStaff/staffTimeline only). The form scaffold P2 "extends — do NOT rebuild" does not exist. Either Plan-1 P5 UI must land first (verify before P2 starts) or P2's effort includes building the scaffold. The phase's "re-read staff-profile.tsx at execution" hedge is good — make it a hard gate. | grep profileUpsert/rateCreate in apps/admin/src = 0 hits; staff-profile.tsx:34-37 |
| M5 | MAJOR | P4 | **Director drill-down via `history` will be FORBIDDEN.** `canViewStaffPunch` (`check-in-out.ts:42-48`) allows hr / self / direct managerId only — directors are not covered (super_admin aside). P4 §6.3 has directors drilling into any facility staff's punches by "reusing history (:250)". Breaks the moment target's managerId is null-auto-resolved to the *other* director or set to a lead. Extend the guard (e.g. allow roles holding checkInOut.monthlyReport) or give monthlyReport its own drill-down query. Same manager-only filter exists in `manualPunchPendingItems` (`dashboard.ts:125-132`) — after P2 sets managerId to leads, manual-punch approvals leave the director inbox entirely; verify that's intended under D5. | check-in-out.ts:42-48,258-267; dashboard.ts:125-132 |
| M6 | MAJOR | P4 | **No ICT month-bucketing exists; "reuse :20-36 helpers" doesn't cover it.** `lateMinutes`/`earlyLeaveMinutes` are per-punch minute calcs (with a `(h+7)%24` wrap that misdates punches near midnight VN / night shifts). Month filters everywhere are UTC: `periodRange` (`payroll.ts:93-99`), `history` (`check-in-out.ts:271`), `registeredInMonth` (`shift-registration.ts:158-159`). Punches 00:00–06:59 VN on the 1st land in the wrong month. Also note the commission auto-feed buckets by UTC `approvedAt` — pick and document ONE convention for the payslip period (suggest ICT boundaries = UTC(y,m-1,1)−7h) and add month-edge + missing-checkout (single-punch day, `todayStatus` logic :164-181 gives early=0) + >2-punch-day aggregation rules. These rules are currently undefined money math. | check-in-out.ts:20-36,105-113,164-190; payroll.ts:93-99 |
| M7 | MAJOR | P5 | **Sale gets `afterSale.assign` but not `user.listAssignableForAfterSale`.** The assign dropdown calls it (`cskh-panel.tsx:97-99`); the perm (`permissions.ts:241`) is cskh/gd_kd only, and its own comment says it must track the afterSale.assign role set. Without the 4th grant, sale's assign flow throws FORBIDDEN in the UI. This also breaks P6's "snapshot diff == exactly the 3 permission grants" acceptance line — the diff will legitimately be 4 modules; reword P6. | permissions.ts:238-241; cskh-panel.tsx:97-99 |
| M8 | MAJOR | P2 | **managerId input has zero validation planned.** Nothing prevents managerId = self (packets then route to self → `assertAssignedApprover` self-block :91 makes them approvable only by nextManagerId/super_admin), A↔B mutual manager pairs (mutual approval, collusion), cross-facility / inactive / nonexistent UUIDs (loose no-FK column, schema.prisma:1336-1337; `resolveManager` routes to whatever is there). Required guards in profileUpsert: managerId ≠ userId, target is active co-facility staff; decide and document the A↔B stance (bounded — resolveManager only walks 2 levels, so no infinite loop, but approval-authority loop exists). | schema.prisma:1334-1337; shift-registration.ts:21-59,86-101 |
| N1 | MINOR | P1/P2 | **Masking has no reachable non-privileged reader today.** Every client-facing EmploymentProfile read is `profileList` (`payroll.ts:381-387`, roles = the 2 directors = exactly `canReadSensitiveHr`); hr/staff have no profile-read procedure; `audit.timeline` whitelist excludes employment_profile (`audit.ts:12-24`) and `staffTimeline` reads `user` events only (:147-159). So the masking matrix as specified tests nothing real. Either name the actual future surface (e.g. HR read grant, staff self-view) or record that mask-on-read is defense-in-depth for profileList's unselected `findMany` (which WILL return the raw new columns by default — that call site must get an explicit select/mask). | payroll.ts:381-387; audit.ts:12-24,147 |
| N2 | MINOR | P2 | `profileUpsert` already accepts `startedAt` (`payroll.ts:335,360,369`) — Plan-1 landed it. P2 requirement "input += managerId, startedAt" is half-stale; only managerId + 4 sensitive columns are new. Also confirmed: `assertCanManagePayrollTarget` self-block (:37) + domain scoping (:40-44) automatically cover the new fields — P2's assumption here is TRUE. | payroll.ts:35-45,327-379 |
| N3 | MINOR | P3 | **Sale can create drafts but can never see any receipt** (`finance.receiptList` excludes sale, `permissions.ts:130`; finance nav gated on receiptList, `nav-permissions.ts:79`). Draft-only is D2-intended, but the sale has no status feedback beyond the create-response toast, and the "create flow" must be fully self-contained in opportunity-detail (it cannot reuse finance-panel's ReceiptCreateCard surface/refresh which assumes receiptList). Data deps for the prefill modal are OK for sale: course/classBatch/student list are protectedProcedure; price resolves server-side; voucher is a text input. Confirm the no-visibility UX with the operator or grant a scoped "own drafts" list. | permissions.ts:126-136; nav-permissions.ts:79; finance-panel.tsx:662-753 |
| N4 | MINOR | P3 | **kind='new' forcing vector:** treating any surviving linked opp as O5 makes every linked receipt 'new' (higher commission rate than renewal). A sale can attach a fresh same-name opportunity to a renewal receipt to reclass it. This exists today if the opp is manually walked to O5; auto-advance just removes friction. The comment at `finance.ts:608-611` shows this is accepted design for win-backs — but P6's int tests should add the adversarial case (renewal + fresh linked opp) and the approver UI should show kind before approve. | finance.ts:608-648 |
| N5 | MINOR | P4 | Team-lead approvers get notif (P4 fix) but no approvals inbox: `dashboard.myApprovals` is directors-only (`permissions.ts:66`), cockpit approve buttons are director-only panels. Leads discover packets via `shift-reg-list-panel` (staff have `shiftRegistration.list`, approve button at :58) — verify that panel actually renders the approve action for a non-director assigned manager; otherwise D5 is API-reachable but UI-dead for leads. | permissions.ts:62-67,253-263; shift-reg-list-panel.tsx:58 |
| N6 | MINOR | P4 | `payslipReopen` (`payroll.ts:744`) returns a finalized slip to draft → recompute re-aggregates penalties from live punch data (manual punches approved later change the number). Acceptable if documented in 0025 as "finalize freezes; reopen re-derives", but say it explicitly — "freeze like other payslip inputs" implies a snapshot store the plan doesn't build. | payroll.ts:735-750 |
| N7 | MINOR | P6 | Decision-id race: plan pins 0024-0027 as "next free". Other in-flight plans may also mint 0024+. Re-check `docs/decisions/` latest id at execution, not planning, time. Snapshot: fixture (`apps/api/test/fixtures/permission-snapshot.json`) already reflects Plan-1's payroll re-own — P6's single-regen approach is sound; adjust "exactly 3" → "exactly the intended grants (4 modules incl. listAssignableForAfterSale)". | fixtures/permission-snapshot.json:84-137 |

## Anchor verification (plan claims vs working tree)

Verified ACCURATE: `permissions.ts:131` receiptCreate, `:28-34` afterSale, `:259-261` withdraw/approve/reject, `:277` monthlyReport (perm live, procedure absent from `check-in-out.ts` — confirmed dead-perm as claimed); `finance.ts:198,258` opportunityId accepted; `:608-668` attribution + `:648` kind ordering trap exactly as described (fix ordering IS implementable at those lines); `finance-panel.tsx:714,727` no opportunityId sent; `opportunity-detail.tsx` has zero finance/receipt code (clean host); `user.ts:83-92` no phone, `:116-121` facility guard non-superadmin-only, raw P2002 on dup email; `crm.ts:160` contactList 0 callers, `:71-100` silent dedupe; `check-in-out.ts:175-182` display-only penalty, `:250` history; `shift-registration.ts:17-58,86-101,328-336` all exact; `schema.prisma:1324-1345` managerId/startedAt exist, 4 columns absent; `App.tsx:267` UserCreateModal. Plan-1 payroll re-own to 2 directors already in tree (`permissions.ts:171-198`) — P4/P5 baseline consistent, no conflict found there.

Serialization check (vector 6): P3/P4/P5 permissions.ts ordering is fine; no other shared file collision found between P2-P5 (App.tsx only P2, finance-panel only P3, staff-profile only P2). Cross-plan 260702-1007 confirmed disjoint (submission/lms files). The real missed "shared" artifacts are `packages/domain-payroll/*` and `schema.prisma` needed by P4 (C1) — currently owned by nobody/P1.

## Recommended plan edits (priority order)

1. C1: add Payslip penalty columns (amount, note, override, override reason/actor) to P1's migration OR create a P4a schema step; add `packages/domain-payroll/src/payslip.ts` (+ tests) to P4 ownership; pin pre-tax vs post-tax in decision 0025.
2. C2: reword P4 step 2 — aggregation lives in `assembleSlipData`; penalty override is its own field/proc, not variablePay.
3. M1: pick a rule (director bypass in guard vs 0027 rewrite) and update P4 step 1 + test matrix + dashboard inbox filter.
4. M2/M3: P3 step 4 — extend opp select (closedAt, lostReason), skip advance on lost, stamp closedAt on advance; add cancel-path rule.
5. M4: make "seam-fixes P5 forms exist" a hard entry gate for P2 with a fallback scope line.
6. M5/M6: P4 — extend canViewStaffPunch (or dedicated drill-down proc); define ICT bucketing + punch-pairing rules.
7. M7: add `user.listAssignableForAfterSale += sale` to P5; fix P6 snapshot wording.
8. M8: add managerId validation to P2 requirements.

## Unresolved questions (for operator/planner)

1. Penalty position in tax math: pre-tax (reduces taxable income) or post-net? (C1 — needs a business answer, decision 0025.)
2. D5 intent: should BOTH directors always be able to approve any packet (0027 as drafted) or only assigned + next-level? Code currently supports only the latter. (M1)
3. Cancelled receipt on an auto-won opp: auto-revert stage, or allow manual regression? (M3)
4. Is sale's zero visibility of own drafts acceptable, or grant a scoped own-receipts list? (N3)

---
Status: DONE
Summary: Adversarial review of all 6 phases against the current working tree; verdict FIX-FIRST with 2 critical (payslip deduction has no schema/domain landing zone; override path wipes the penalty), 8 major, 7 minor findings, all file:line-verified.
