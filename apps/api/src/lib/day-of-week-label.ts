/** Vietnamese weekday labels indexed by dayOfWeek (0=CN..6=T7) — the convention used by
 * ScheduleSlot/ClassSession.dayOfWeek and enumerateSessions' getUTCDay() across the app. */
export const DOW_LABEL = ['CN', 'T2', 'T3', 'T4', 'T5', 'T6', 'T7'] as const;
