node_modules/
dist/
build/
.turbo/
coverage/
*.log
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
.vite/

# test/automation artifacts
.playwright-mcp/

# local content-addressed PDF store (S1.7; swaps to MinIO later)
.data/

# flow harness local telemetry/workspaces
.flow/
# Gemini CLI settings (contains user-specific config)
.gemini/
# local AI-agent harness config (Claude Code)
.claude/

# scratch verify artifacts (screenshots, playwright aria snapshots) — not source
*-verify.png
*-verify.yml
cv5-commission-card.yml
# legacy old-system export, not part of this greenfield repo
release-manifest.json

# Harness durable layer
harness.db
harness.db-wal
harness.db-shm
scripts/bin/harness-cli
scripts/bin/harness-cli.exe
.env.local-docker
.gitnexus

# Root-level QA/debug screenshots (throwaway)
/*.png

# Email template preview output (dev QA)
apps/api/email-previews/

# repomix packed-context output (regenerable, not source)
repomix-output.*
repomix-product-source.md
